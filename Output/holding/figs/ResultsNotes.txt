Comp1: change in migration when het = .4-.6
het == 1 per > 3x > 1x > 0
Fst == 0 > 1x > 3x > 1 per

Comp2: change in source het when mig = 1 per gen  // [note: 2 weird years at mid het]
het == high >= low > mid > 0
Fst == high > 0 > low >= mid
WHEN 1 MIG PER GEN, GREATER *DIFFERENCE* IN SOURCE HET (EITHER HIGH OR LOW) MAINTAINS HETEROZY
WHEN 1 MIG PER GEN, GREATER SOURCE HET DIVERGES FROM ORIGINAL POP FASTEST, THEN NO MIG

Comp3: change in source het when mig = 1x of 50 indv
het == high > mid > low > 0
Fst == 0 > high > mid >= low
WHEN 1X PULSE MIG, GREATER SOURCE HET MAINTAINS POP HET.
WHEN 1X PULSE MIG, NO MIG AND THEN GREATER SOURCE HET DIVERGES FROM ORIGINAL POP FASTEST.

Comp4: change in source het when mig = 3x of 25 indv
het == high > low > mid > 0
Fst == 0 > high > mid >= low
WHEN 3X PULSE MIG, GREATER *DIFFERENCE* IN SOURCE HET MAINTAINS POP HET.
WHEN 3X PULSE MIG, NO MIG AND THEN GREATER SOURCE HET DIVERGES FROM ORIGINAL POP FASTEST.

Comp5: change in migration when het = .8-.9
het == 1 per > 3x > 1x > 0
Fst == 1 per > 0 >= 1x > 3x

Comp6: change in migration when het = .1-.2
het == 1 per > 3x > 1x > 0
Fst == 0 > 1x > 3x > 1 per

AT ALL SOURCE HETEROZYGOSITIES, THE TREND FOR HO AT ALL MIGRATIONS INTENSITIES ARE THE SAME (1 per is best, then 3x, then 1x, then none)
POP DIVERGENCE FROM STARTING POP IS SLOWEST WHEN 1 MIG PER GEN WHEN SOURCE HET IS LOW AND MID BUT WHEN SOURCE HET IS HIGH, PULSE MIGRATION AT 3X SLOWS DIVERGENCE FROM ORIGINAL POP BEST


1 per == one migrant per generation, selected at random
3x == 3 pulse migrations of 25 indv each at years 175, 201, 225
1x == 1 pulse migration of 50 indv at year 175

{original focal pop het between .4-.6}

 